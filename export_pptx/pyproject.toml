# This project was generated with 0.3.19 using template: https://github.com/beeware/briefcase-template@v0.3.19
[tool.briefcase]
project_name = "ExportPPTX"
bundle = "hmoozoo.com"
version = "0.0.1"
url = "https://github.com/pyhteam/PythonPPTX"
license.file = "LICENSE"
author = "Ma Seo Sen"
author_email = "senms9x@gmail.com"

[tool.briefcase.app.export_pptx]
formal_name = "Export PPTX"
description = "Library for creating PowerPoint presentations"
long_description = """More details about the app should go here.
"""
sources = [
    "src/export_pptx",
]
test_sources = [
    "tests",
]

console_app = true
requires = [
]
test_requires = [
    "pytest",
]

[tool.briefcase.app.export_pptx.macOS]
universal_build = true
requires = [
]

[tool.briefcase.app.export_pptx.linux]
requires = [
]

[tool.briefcase.app.export_pptx.linux.system.debian]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.export_pptx.linux.system.rhel]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.export_pptx.linux.system.suse]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.export_pptx.linux.system.arch]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.export_pptx.linux.flatpak]
flatpak_runtime = "org.freedesktop.Platform"
flatpak_runtime_version = "23.08"
flatpak_sdk = "org.freedesktop.Sdk"

[tool.briefcase.app.export_pptx.windows]
requires = [
]

# Mobile deployments
[tool.briefcase.app.export_pptx.iOS]
supported = false

[tool.briefcase.app.export_pptx.android]
supported = false

# Web deployments
[tool.briefcase.app.export_pptx.web]
supported = false

